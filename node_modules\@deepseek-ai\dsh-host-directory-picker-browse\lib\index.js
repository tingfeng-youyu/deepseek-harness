# 漏洞报告：DSH Web 目录选择器存在未限制的目录遍历/文件系统浏览

## 基本信息

- **项目**: DeepSeek Harness (DSH)
- **受影响版本**: `0.1.0-rc.6`（`@deepseek-ai/dsh`）
- **受影响组件**: `@deepseek-ai/dsh-host-directory-picker-browse`
- **漏洞类型**: 路径遍历 / 未限制的目录列表访问 (Path Traversal / Unrestricted Directory Listing)
- **严重程度**: 中-高（取决于部署环境；如暴露于公网则为高危）
- **报告语言**: 中文 / English

---

## 漏洞描述

DSH 的 Web UI 中的目录选择器（directory picker）允许浏览器端传入任意绝对路径进行目录列表查询。服务端 `list()` 方法仅做 `resolve(path ?? home)` 后直接调用 `opendir(target)`，**没有任何校验限制 `target` 必须位于某个安全根目录（如工作区）内**。

这意味着通过浏览器 UI，用户可以列出整台机器的任意目录内容（如 `C:\Windows\System32`、`D:\` 等），造成敏感文件泄露风险。

---

## 受影响代码路径

**文件**: `apps/cli/node_modules/@deepseek-ai/dsh-host-directory-picker-browse/lib/index.js`

**问题代码**（`list` 方法，约第 165-218 行）:

```javascript
async list(path, signal) {
    const home = homedir();
    if (path !== void 0 && !fullyQualified(path)) 
        throw new DirectoryPickerError("directory-unreadable", path, ...);
    const target = resolve(path ?? home);  // ← 仅做 resolve，无根目录限制
    // ... opendir(target) 递归列出所有子目录
}
```

**对比**: 同文件中的 `createDirectory` 方法至少限制了 `name` 不能包含路径分隔符，但 `list` 完全没有边界检查。

---

## 复现步骤

1. 启动 DSH Web 服务：`dsh --profile web`
2. 在浏览器中打开 DSH UI（默认 `http://127.0.0.1:3080`）
3. 使用目录选择器功能，输入或导航到工作区外的路径（如 `C:\`、`D:\`、`C:\Users\`）
4. 服务端返回该目录的完整列表，包括子目录面包屑导航
5. 可继续进入任意子目录，无边界限制

---

## 影响

- 攻击者可枚举服务器文件系统结构
- 可能泄露敏感配置文件、密钥、源代码等
- 结合其他漏洞（如文件读取）可造成更严重的信息泄露

---

## 修复建议

在 `list()` 方法中增加根目录白名单/沙箱校验，例如：

```javascript
const ALLOWED_ROOTS = [resolve(homedir()), resolve(process.cwd())];
const target = resolve(path ?? home);
if (!ALLOWED_ROOTS.some(root => target === root || target.startsWith(root + sep))) {
    throw new DirectoryPickerError("directory-unreadable", target, "Access denied: outside allowed roots");
}
```

---
